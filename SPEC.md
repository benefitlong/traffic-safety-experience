# 체험형 교통안전교육 프로그램 명세

## 원칙과 목표

다양한 연령대와 디지털 활용 수준의 교육생이 설명 없이 쉽게 사용할 수 있는 교통안전 체험 프로그램을 만든다. 말과 숫자만으로 체감하기 어려운 정지거리를 직접 체험하게 한다. 사용자는 운전하지 않으며, 자동차가 자동 주행하다 도로의 장애물이 나타나면 브레이크를 누른다. 실제 반응시간으로 반응거리와 정지거리를 계산한다. 정지거리 관련 수치와 계산 기준은 임의로 변경하지 않는다.

교육생 화면 제목은 **멈출 수 있을까? - 정지거리 편 -**이다.

## 기술 및 범위

- HTML, CSS, JavaScript를 우선 사용하며 불필요한 프레임워크를 사용하지 않는다.
- 외부 API, OpenAI API, 외부 CDN 없이 완전 오프라인으로 작동한다.
- GitHub Pages 및 PC·모바일 브라우저를 고려한 반응형 구조로 만든다.
- 이번 범위는 Phase 1 핵심 정지거리 체험뿐이다.
- 사전·사후 설문, 두 번째 속도 체험, 교수용·관리자용 기능, QR 및 배포는 구현하지 않는다.

## 화면 흐름

### 첫 화면

다음 문구와 큰 버튼만 사용하며 속도와 정지거리의 관계, 학습 내용 또는 정답을 암시하지 않는다.

- 체험형 교통안전교육 프로그램
- 멈출 수 있을까?
- - 정지거리 편 -
- 체험 시작하기

### 첫 체험 안내

첫 체험 속도는 **50km/h 고정**이다.

- 먼저 50km/h로 달려볼게요.
- 앞을 잘 보세요.
- 도로에 장애물이 나타나면 브레이크를 눌러주세요.
- 시작하기
- PC에서는 Space 키도 사용할 수 있음을 작게 안내한다.

### 주행

- 2D 측면형 도로 화면이다.
- 차량은 화면 왼쪽 약 25~30%에 거의 고정한다.
- 앞·뒤 바퀴가 회전하고 도로 차선과 배경은 왼쪽으로 이동한다.
- 속도에 따라 바퀴 회전과 배경 이동 속도를 바꿀 수 있는 구조로 한다.
- 상단에 **50 km/h**, 하단에 큰 **🛑 브레이크** 버튼을 표시한다.
- 브레이크 버튼 클릭, 모바일 화면 터치, PC Space 키를 지원한다.

## 장애물과 안전 원칙

사람이나 동물이 아닌 **도로에 떨어지는 화물 상자**를 사용한다. 상자는 주행 시작 3~6초 사이 무작위 시점에 앞쪽에서 도로로 떨어진다. 상자가 떨어지기 시작한 때가 아니라 도로 위 위험 위치에 도달해 장애물로 인식할 수 있는 순간부터 반응시간을 측정한다.

상자 등장 시 “위험!” 문구, 브레이크 강조, 경고 플래시, 경고음을 사용하지 않는다. 보행자 충돌, 사람·동물 사고, 차량과 상자 충돌 장면, 파손, 사고음, 충격적인 화면, 게임오버도 사용하지 않는다. 상자는 중립적 시각 신호다.

## 반응시간

상자가 위험 위치에 도달한 순간을 `reactionStart`로 기록하고 브레이크 작동 시각과의 차이를 밀리초 정밀도로 저장한다. 화면에는 초 단위 소수점 둘째 자리로 표시한다. 상자 도착 전 입력은 반응시간으로 인정하지 않고 필요하면 **장애물이 나타나면 눌러주세요.**라고 짧게 안내한다.

## 정지거리 계산 기준

FHWA/AASHTO 정지시거 산정 방식을 기준으로 한다.

- 기준 반응시간: **2.5초**
- 기준 감속도: **3.4m/s²**
- 속도 범위: **30~100km/h**
- 반응거리 = `0.278 × 속도(km/h) × 반응시간(초)`
- 제동거리 = `0.039 × 속도² ÷ 3.4`
- 정지거리 = `반응거리 + 제동거리`

개인 체험에는 2.5초 대신 실제 측정 반응시간을 사용한다. 내부 계산 정밀도를 유지하고 화면에는 거리를 소수점 첫째 자리까지 표시한다.

| 속도 | 기준 반응거리 | 기준 제동거리 | 기준 정지거리 |
|---|---:|---:|---:|
| 30km/h | 20.9m | 10.3m | 31.2m |
| 40km/h | 27.8m | 18.4m | 46.2m |
| 50km/h | 34.8m | 28.7m | 63.4m |
| 60km/h | 41.7m | 41.3m | 83.0m |
| 70km/h | 48.7m | 56.2m | 104.9m |
| 80km/h | 55.6m | 73.4m | 129.0m |
| 90km/h | 62.6m | 92.9m | 155.5m |
| 100km/h | 69.5m | 114.7m | 184.2m |

## 제동과 결과

브레이크 후 즉시 멈추지 않는다. 정상 주행 중에는 차량 위치를 유지하고, 브레이크 입력 후에는 차량이 화면 오른쪽으로 계속 전진하면서 바퀴, 도로, 배경 이동이 1.5~2.5초 안에 부드럽게 감속한다. 자동차가 뒤로 밀리거나 후진하는 것처럼 표현하지 않는다. 이 연출 시간은 공식 계산값과 무관하다.

브레이크를 누른 순간의 도로 위치에는 **▼ 브레이크 기준** 표시를 남긴다. 차량은 그 지점에서 앞으로 이동하고, 최종 위치에 **● 정지** 표시를 남긴다. 두 표시 사이를 도로 위 구간으로 연결해 브레이크 후에도 이동한 거리를 직관적으로 보여준다.

브레이크 입력 직후 외부 파일이나 네트워크에 의존하지 않는 짧은 제동 효과음을 재생한다. 차량 위치값은 제동 중 이전 프레임보다 감소하지 않으며, 왼쪽 주행 위치에서 화면 중앙 부근까지 진행 방향으로만 이동한다.

정지 후 결과의 중심은 전체 정지거리다. **50km/h로 달렸을 때**, **위험을 발견한 뒤 완전히 멈추기까지**, **약 XX.Xm**, **이동했습니다.**, **자세히 보기**를 표시하고 위험 발생 지점부터 최종 정지 위치까지 거리를 크게 시각화한다. 반응시간은 메인 결과에 표시하지 않는다.

상세 화면은 쉬운 표현으로 다음을 보여준다.

- 왜 이만큼 이동했을까요?
- 위험을 보고 브레이크를 누르기 전까지 XX.Xm
- 브레이크를 누른 뒤 완전히 멈출 때까지 XX.Xm
- 전체 XX.Xm
- 체험 중 측정된 반응시간(작은 보조정보)
- 이 전체 거리를 '정지거리'라고 합니다.

반응시간을 빠르다/느리다, 점수, 등급, 성공/실패로 평가하지 않는다. 교육목표는 반응속도 평가가 아니라 자동차 속도에 따른 정지거리를 체감하는 것이다.

도로 위 시각화에서 반응 중 이동과 제동 중 이동을 다른 구간으로 구분하며, 30~100km/h 결과가 한 화면에 들어오도록 눈금을 자동 조절한다.

## 디자인, 접근성, 사용성

- 세련된 교육용 2D 일러스트와 브랜드를 연상시키지 않는 일반 승용차, 도심 도로를 사용한다.
- 지나치게 어린이용인 스타일, 복잡한 계기판과 게임 HUD를 피한다.
- 한 화면에서 한 행동만 요구하고 큰 글씨와 모바일에서 누르기 충분한 큰 버튼을 쓴다.
- 색상만으로 중요 정보를 전달하지 않고 충분한 텍스트 대비를 확보한다.
- 화면 크기가 달라도 주요 버튼과 문구가 잘리지 않게 한다.
- 애니메이션은 부드럽고 과하지 않게 사용한다.

## Phase 1 완료 조건

첫 화면, 50km/h 안내, 2D 자동주행, 고정 차량, 회전 바퀴, 이동 배경, 3~6초 무작위 상자, 위험 위치 도달 시 측정 시작, 버튼·터치·Space 입력, 실제 반응시간, 세 거리 계산, 부드러운 감속, 결과, 상세, 거리 시각화, 완전 오프라인 실행을 완성한다.

구현 후 오프라인 실행, 조기 입력 무효화, 상자 등장 시 경고 미표시, 공식에 맞는 50km/h 계산, 모바일 버튼, Space·터치, 자연스러운 이동과 감속을 직접 확인한다.

## Phase 2: 속도 선택 및 정지거리 비교

- 첫 번째 50km/h 체험 후 30~100km/h 중 다른 속도를 큰 버튼으로 선택한다. 두 번째 체험을 선택할 때 50km/h는 완료 표시와 함께 비활성화한다.
- 선택 속도에 50km/h 대비 비율을 적용해 바퀴, 도로, 배경의 움직임을 표현한다.
- 모든 체험은 Phase 1과 같은 장애물, 입력, 반응시간 측정, 계산식, 전진 제동 애니메이션을 사용한다.
- 체험 결과는 속도, 반응시간, 공주거리, 제동거리, 정지거리, 체험 순서와 함께 누적 저장한다.
- 두 번째 체험 이후 첫 50km/h와 가장 최근 속도를 비교한다.
- 메인 비교에는 개인 반응시간을 사용하지 않는다. 두 속도 모두 기준 반응시간 2.5초와 감속도 3.4m/s²를 적용한 기준 데이터를 사용한다.
- 비교 막대는 더 긴 정지거리를 100%로 하는 동일 축척을 사용한다.
- 속도 배수와 기준 정지거리 배수는 선택 결과에서 자동 계산해 소수점 첫째 자리로 표시한다.
- 개인 체험 결과는 별도 보조 화면에서 공주거리, 제동거리, 정지거리 중심으로 제공한다. 반응시간은 작은 보조정보로만 표시하며 평가하지 않는다.
- 두 번의 필수 체험 이후에는 완료된 속도를 표시하면서 횟수 제한 없이 자유 탐색할 수 있다.
- 완료 화면은 Phase 3 질문을 추후 연결하기 쉽도록 별도 화면으로 둔다.

기준 정지거리는 30: 31.2m, 40: 46.2m, 50: 63.4m, 60: 83.0m, 70: 104.9m, 80: 129.0m, 90: 155.5m, 100: 184.2m를 변경하지 않는다.

## Phase 3: 사전·사후 교육효과 측정

- 전체 흐름은 시작, 사전 안내, 사전 질문 3개, 기존 체험과 비교, 사후 안내, 동일한 사후 질문 3개, 학습내용 정리, 완료 순서다.
- 사전·사후 질문은 별도 `survey.js` 모듈에서 동일한 문항 원본으로 제공한다. 향후 설정으로 전체 질문을 건너뛸 수 있어야 한다.
- 한 화면에 한 문항과 `1 / 3` 형태의 번호만 표시한다. 네 개의 큰 선택지 중 하나를 탭하면 약 0.3초 선택 상태를 보인 뒤 자동 이동한다.
- 질문 중에는 답의 옳고 그름이나 학습내용을 알려주지 않는다.
- 응답은 개인정보 없이 세션 안에서 `pre.q1~q3`, `post.q1~q3`에 1~4의 일관된 선택지 번호로 저장한다.
- 사후 질문이 끝난 뒤 브레이크를 밟아도 바로 멈추지 않는다는 점과 속도가 높아질수록 정지거리가 크게 늘어난다는 점을 처음으로 명확히 정리한다.
- 학습 정리에서는 동일 축척으로 50km/h 63.4m와 100km/h 184.2m를 비교하고 기준 반응시간 2.5초, 감속도 3.4m/s²를 표시한다.
- 실제 자동차 행동은 `브레이크를 밟다`, 프로그램 조작은 `브레이크 버튼을 누르다`로 표현한다.
- 설문 응답은 기존 체험 배열과 별도로 관리하지만 같은 세션에서 함께 읽을 수 있게 한다. 서버로 전송하지 않는다.
- 교수용 메뉴, 관리자 통계 화면, CSV 저장, 배포 기능은 이 단계에 포함하지 않는다.

## Phase 4: 교수용 메뉴

- 첫 화면의 작은 `교수용` 버튼에서 교육생 체험과 분리된 교수용 화면으로 진입한다.
- 교수용 화면은 속도 비교, 기준표, 근거자료, 강의 포인트의 네 메뉴를 항상 접근 가능한 탭으로 제공한다.
- 속도 비교에서는 30~100km/h 중 두 속도를 선택하고 기준 공주거리, 제동거리, 정지거리, 거리 차이, 속도 배수, 정지거리 배수를 표시한다. 두 막대는 더 긴 거리를 100%로 하는 동일 축척을 사용한다.
- 기준표는 명세의 8개 기준 행을 변경 없이 표시하고 기준 반응시간 2.5초와 감속도 3.4m/s²를 명시한다.
- 근거자료에는 거리 정의, 공식, 설계 기준값의 의미, AASHTO 및 FHWA 출처, 실제 조건에 따른 차이 안내를 내부 텍스트로 제공한다.
- 강의 포인트는 위험 인지 중 이동, 제동 중 이동, 속도 증가에 따른 비선형적 정지거리 증가, 50/100km/h 비교를 네 개의 큰 카드로 제공한다.
- 교수용 데이터와 화면 로직은 `instructor.js`에 분리하고 서버나 외부 인터넷을 사용하지 않는다.
- `처음으로` 버튼은 교육생 세션을 변경하지 않고 메인 화면으로 돌아간다.
- 관리자 통계, CSV 저장, 배포는 이 단계에 포함하지 않는다.

## Phase 5: 관리자 메뉴

- 첫 화면의 작은 `관리자` 버튼은 비밀번호 화면을 거쳐 교육생·교수용 흐름과 분리된 관리자 화면으로 진입한다. 초기 비밀번호는 `admin.js`의 별도 상수로 관리한다.
- 관리자 화면은 효과성 측정 설정, 체험 설정, 결과 통계, 데이터 관리의 네 고정 탭을 제공한다.
- 설정은 `stoppingDistanceSettings`, 익명 세션 기록은 `stoppingDistanceSessions` 키로 `localStorage`에 분리 저장한다.
- 기본 설정은 설문 ON, 30~100km/h 사용, 첫 속도 50km/h, 효과음 ON, 완료 25초 후 자동 복귀 ON이다. 50km/h는 항상 활성화한다.
- 체험 시작부터 완료까지를 한 익명 세션으로 취급하고 `SESSION-000001` 형태의 ID, 일시, 사전·사후 응답, 체험 속도, 개인 거리 결과, 체험 횟수를 저장한다. 개인정보는 수집하지 않는다.
- 통계는 총 세션 수, 총 체험 횟수, 가장 많이 선택한 추가 속도, 설문 완료 세션의 문항별 사전·사후 정답률과 %p 변화, 전체 평균 정답률, 응답 분포, 추가 속도 횟수를 제공한다.
- 반응시간 순위나 개인 평가는 제공하지 않는다.
- 데이터 목록은 익명 세션 정보만 표시한다. CSV는 UTF-8 BOM과 세션·설문·체험 열을 포함해 브라우저에서 생성한다.
- 전체 데이터 삭제는 확인 후 세션 데이터만 삭제하며 설정은 유지한다. 설정 초기화는 별도 확인 후 기본값으로 되돌린다.
- 관리자 기능은 `admin.js`에 분리하며 서버, 외부 데이터베이스, API, CDN, 인증서비스, 차트 라이브러리를 사용하지 않는다.
- GitHub 배포, 여러 기기의 자동 수집, 클라우드 연동은 이 단계에 포함하지 않는다.

## Phase 6: 전체 디자인/UI 최종 개선

- 전체 디자인은 공공기관 교통안전교육의 신뢰감과 직관적인 체험 콘텐츠를 결합한 네이비·블루 중심 체계로 통일한다.
- 공주거리는 블루, 제동거리는 노랑·주황, 정지거리는 진한 네이비로 교육생·교수용 화면 전반에 일관되게 표시한다.
- 버튼은 Primary, Secondary, Utility 역할에 따라 크기와 강조도를 통일하고 카드의 테두리·그림자·둥근 모서리는 절제한다.
- 자동차는 외부 이미지 없이 기존 HTML/CSS 구조를 유지하면서 길고 낮은 일반 승용차 비율, 자연스러운 캐빈과 유리 경사, 동일 크기 바퀴로 정돈한다. 창문은 캐빈 경계 안에 배치한다.
- 도로와 도심 배경은 자동차·상자·브레이크 순으로 시선이 흐르도록 채도와 장식을 낮춘다.
- 설문은 문항 번호, 질문, 네 선택지에 집중하며 결과와 비교 화면은 거리(m)를 가장 강하게, 배수는 두 번째 정보로 표현한다.
- 교수용은 자료 가독성, 관리자용은 간결한 업무형 카드·표·토글을 우선한다.
- 360px 모바일에서도 자동차, 상자, 브레이크, 설문 선택지, 좌우 보조 메뉴가 화면 경계를 벗어나지 않아야 한다.
- 외부 글꼴, 이미지, SVG, CDN, 아이콘 라이브러리, JavaScript, CSS를 추가하지 않는다.
- Phase 1~5의 계산, 데이터, 설문, 애니메이션, 저장, 메뉴와 화면 전환 로직은 변경하지 않는다.
