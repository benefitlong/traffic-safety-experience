# Pretendard 로컬 폰트

이 디렉터리에 공식 `PretendardVariable.woff2` 파일을 넣어 주세요.

예상 경로:

`assets/fonts/PretendardVariable.woff2`

`phase6.css`의 `@font-face`가 이 상대경로를 사용합니다. 폰트 파일이 없거나 로딩에 실패하면 `Noto Sans KR`, `Apple SD Gothic Neo`, `Malgun Gothic`, `sans-serif` 순서로 대체됩니다.
